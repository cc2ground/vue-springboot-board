apiVersion : apps/v1
kind: Deployment
metadata:
  name: "aksclusterghworkshop"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "ghworkshop"
  template:
    metadata:
      labels:
        app: "ghworkshop"
    spec:
      containers:
        - name: "board-api"
          image: "acrghworkshop.azurecr.io/board-app"
          ports:
          - containerPort: 8090