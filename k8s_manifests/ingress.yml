apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ghworkshop
  annotations:
    kubernetes.io/ingress.class: addon-http-application-routing
spec:
  rules:
  - host: ghworkshop.7e0cd4fae80246bb9979.koreacentral.aksapp.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service: 
            name: ghworkshop
            port: 
              number: 8090